const authDAO = require('./auth')
const brandDAO = require('./brand')
const productDAO = require('./product')
const categoryDAO = require('./category')
const reviewDAO = require('./review')

module.exports = {
	authDAO,
	brandDAO,
	productDAO,
	categoryDAO,
	reviewDAO,
}